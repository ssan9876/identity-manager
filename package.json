{
  "name": "identity-manager",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "setup": "node scripts/setup.mjs",
    "bootstrap:admin": "pnpm --filter @idm/api run bootstrap:admin --",
    "dev": "node scripts/dev.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
